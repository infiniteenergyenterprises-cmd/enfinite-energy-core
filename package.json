{
  "name": "enfinite-energy",
  "version": "1.0.0",
  "description": "Enfinite Energy – Full-stack solar solutions platform (Next.js frontend + Node.js/Express backend).",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
